local config = {
    Enabled = true,
    Debug = true
}

local description = {
    Enabled = "Set to true to enable the mod, set to false to disable it.",
    Debug = "Enable logging onto command line."
}

return config, description